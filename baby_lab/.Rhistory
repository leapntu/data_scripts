df <- read.csv("df.csv")
library(plyr)
df <- read.csv("df.csv")
diff <- ddply(df, c('id'), transform, t = function(x) {
words = x[x$type == 'word',]$looking
parts = x[x$type == 'part',]$looking
data.frame(t = t.test(words, parts))
})
diff <- ddply(df,'id', transform, t = function(x) {
words = x[x$type == 'word',]$looking
parts = x[x$type == 'part',]$looking
data.frame(t = t.test(words, parts))
})
df
head(df)
diff <- ddply(df,'id', function(x) {
words = x[x$type == 'word',]$looking
parts = x[x$type == 'part',]$looking
data.frame(t = t.test(words, parts))
})
diff <- ddply(df,'id', function(x) {
words = x[x$type == 'word',]$looking
parts = x[x$type == 'part',]$looking
data.frame(t = t.test(words, parts)$p.value)
})
diff
library(plyr)
df <- read.csv("df.csv")
diff <- ddply(df,'id', function(x) {
words = x[x$type == 'word',]$looking
parts = x[x$type == 'part',]$looking
data.frame(t = t.test(words, parts)$statistic)
})
diff
hist(diff$t)
